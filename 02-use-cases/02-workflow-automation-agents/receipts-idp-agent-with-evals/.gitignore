# Secrets / local env
.env

# AgentCore CLI state + generated target file
agentcore/aws-targets.json
.cli/
.agentcore/

# CDK
agentcore/.cache/
cdk-outputs.json
agentcore/cdk/node_modules/
agentcore/cdk/dist/
agentcore/cdk/cdk.out/

# Python
__pycache__/
*.pyc
.venv/
uv.lock

# Evaluation run output
evals/out/

# OS
.DS_Store
# The repository root ignores every lib/ directory, every Dockerfile and .env.*; this
# sample commits its CDK stack, the Runtime's Dockerfile and the env template.
!agentcore/cdk/lib/
!agentcore/cdk/lib/**
!app/receiptsagent/Dockerfile
!.env.example
